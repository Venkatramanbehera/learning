git config --global user.email "Venkatramanbehera1@gmail.com"
git config --global user.name "Venkatramanbehera"

commands
1.git init    :-add the project to git hub
2.git add[file name] || -A    :-  -A used for inserting whole file to the staging area .in this git can understand the file are in git hub
3.git status     :-check which is tracked or untracked.
4.git commit -m "msg"  :-to check the snapshot.where we have to check and commit we can get back to the perticular snapshot and check in this snapshoot file.
5.git remote add origin repoName   :-to used for link the project to the url.
6.git push origin master  :-Used for push the file to the git.
7.git clone repoName   :-Used for download the project in that file.
8.git pull origin master  :-Used to retrive the new add file to the project